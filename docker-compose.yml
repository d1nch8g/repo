version: "3.11"

services:
  app:
    image: fmnx.io/core/repo:latest
    environment:
      PACKREPO_INIT_PKGS: fmnx.io/pkg/gnome-shell-extension-dash-to-dock
      PACKREPO_LOGS_FORMAT: text
      PACKREPO_LOGINS: user|password
    volumes:
      - ./pkgs:/var/cache/pacman/pkg
    ports:
      - 80:80
