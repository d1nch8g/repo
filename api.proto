syntax = "proto3";
package template.v1;
option go_package = "gen/pb";

// Service containing method for adding aur packages
service Aur {
  // Add new package from AUR
  rpc Add(Package) returns (Empty);
  // Check wether some package exists in repository
}

message Package {
  string name = 1;
}

message Empty {}
